import numpy as np
import math
import cmath

sqrt=math.sqrt
cos = math.cos
sin = math.sin
ln = math.log
pi = math.pi
e=cmath.e
phi = (1+sqrt(5))/2

v_ize = np.vectorize
norm = np.linalg.norm
arr = np.array
nan = np.nan
isnan = np.isnan